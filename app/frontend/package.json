{
  "name": "@ft_transcendence/frontend",
  "version": "1.0.0",
  "private": true,
  "description": "Frontend of transcendence app.",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "build": "tsc && tailwindcss -i ./styles/input.css -o ./styles/output.css --minify",
    "clean": "rm -rf dist",
    "start": "npm run build && npm run serve",
    "watch": "tsx watch --clear-screen=false src",
    "dev": "npm run build:initial && npx concurrently \"npm run ts\" \"npm run tw\"",
    "build:initial": "tsc && tailwindcss -i ./styles/input.css -o ./styles/output.css",
    "tw": "tailwindcss -i ./styles/input.css -o ./styles/output.css --watch",
    "ts": "tsc -w --preserveWatchOutput",
    "serve" : "node server.js"
  },
  "dependencies": {
    "@babylonjs/core": "^8.38.0",
    "@babylonjs/gui": "^8.33.2",
    "@babylonjs/loaders": "^8.38.0",
    "@babylonjs/materials": "^8.38.0"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.1.16",
    "http-server": "^14.1.1",
    "tailwindcss": "^4.1.16"
  }
}
