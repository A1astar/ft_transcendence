# Main ModSecurity rules include: base config + CRS
Include "/etc/modsecurity/modsecurity.conf"

# CRS setup and rules
# Paths are set to the commonly used locations in OWASP CRS images.
# Adjust if the image uses a different directory structure.
Include "crs/crs-setup.conf"
Include "crs/rules/*.conf"
